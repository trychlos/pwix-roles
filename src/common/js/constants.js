/*
 * pwix:roles/src/common/js/constants.js
 */

// exported

PR_VERBOSE_NONE = 0;
PR_VERBOSE_CONFIGURE = 0x01 << 0;
PR_VERBOSE_STARTUP   = 0x01 << 1;

// non exported

PACKAGE_NAME = 'pwix:roles';

ROLES_I18N = PACKAGE_NAME;
